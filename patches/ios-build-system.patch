diff --git a/src/Makefile.in b/src/Makefile.in
index a2f7ea0..68f37c4 100644
--- a/src/Makefile.in
+++ b/src/Makefile.in
@@ -313,6 +313,12 @@ NS_OBJ=
 NS_OBJC_OBJ=@NS_OBJC_OBJ@
 ## Used only for GNUstep.
 GNU_OBJC_CFLAGS=$(patsubst -specs=%-hardened-cc1,,@GNU_OBJC_CFLAGS@)
+
+## iosterm.o iosfns.o if HAVE_IOS.
+IOS_OBJ=@IOS_OBJ@
+IOS_OBJC_OBJ=@IOS_OBJC_OBJ@
+IOS_LIBS=@IOS_LIBS@
+
 ## w32fns.o w32menu.c w32reg.o fringe.o fontset.o w32font.o w32term.o
 ## w32xfns.o w32select.o image.o w32uniscribe.o w32cygwinx.o if HAVE_W32,
 ## w32cygwinx.o if CYGWIN but not HAVE_W32, else empty.
@@ -466,8 +472,8 @@ base_obj =
 	itree.o json.o 							       \
 	$(MSDOS_OBJ) $(MSDOS_X_OBJ) $(NS_OBJ) $(CYGWIN_OBJ) $(FONT_OBJ)        \
 	$(W32_OBJ) $(WINDOW_SYSTEM_OBJ) $(XGSELOBJ)			       \
-	$(HAIKU_OBJ) $(PGTK_OBJ) $(ANDROID_OBJ)
-doc_obj = $(base_obj) $(NS_OBJC_OBJ)
+	$(HAIKU_OBJ) $(PGTK_OBJ) $(ANDROID_OBJ) $(IOS_OBJ)
+doc_obj = $(base_obj) $(NS_OBJC_OBJ) $(IOS_OBJC_OBJ)
 obj = $(doc_obj) $(HAIKU_CXX_OBJ)
 
 ## Object files used on some machine or other.
@@ -677,7 +683,7 @@ buildobj.h:
 	done >$@.tmp
 	$(AM_V_at)mv $@.tmp $@
 
-GLOBAL_SOURCES = $(base_obj:.o=.c) $(NS_OBJC_OBJ:.o=.m)
+GLOBAL_SOURCES = $(base_obj:.o=.c) $(NS_OBJC_OBJ:.o=.m) $(IOS_OBJC_OBJ:.o=.m)
 
 gl-stamp: $(libsrc)/make-docfile$(EXEEXT) $(GLOBAL_SOURCES)
 	$(AM_V_GLOBALS)$(libsrc)/make-docfile -d $(srcdir) -g $(doc_obj) > globals.tmp
diff --git a/src/callproc.c b/src/callproc.c
index 8a8cdf8..170b38a 100644
--- a/src/callproc.c
+++ b/src/callproc.c
@@ -32,8 +32,14 @@ Copyright (C) 1985-1988, 1993-1995, 1999-2025 Free Software Foundation,
 #include <sys/file.h>
 #include <fcntl.h>
 
+#ifdef __APPLE__
+#include <TargetConditionals.h>
+#endif
+
 /* In order to be able to use `posix_spawn', it needs to support some
    variant of `chdir' as well as `setsid'.  */
+/* On iOS, posix_spawn_file_actions_addchdir_np is unavailable,
+   so we disable USABLE_POSIX_SPAWN.  */
 #if defined HAVE_SPAWN_H && defined HAVE_POSIX_SPAWN        \
   && defined HAVE_POSIX_SPAWNATTR_SETFLAGS                  \
   && (defined HAVE_POSIX_SPAWN_FILE_ACTIONS_ADDCHDIR        \
@@ -42,7 +48,9 @@ Copyright (C) 1985-1988, 1993-1995, 1999-2025 Free Software Foundation,
   && HAVE_DECL_POSIX_SPAWN_SETSID == 1			    \
   /* posix_spawnattr_setflags rejects POSIX_SPAWN_SETSID on \
      Haiku */						    \
-  && !defined HAIKU
+  && !defined HAIKU                                         \
+  /* posix_spawn_file_actions_addchdir_np unavailable on iOS */ \
+  && !(defined __APPLE__ && TARGET_OS_IPHONE)
 # include <spawn.h>
 # define USABLE_POSIX_SPAWN 1
 #else
diff --git a/src/dispextern.h b/src/dispextern.h
index e6fdb9b..0226fc7 100644
--- a/src/dispextern.h
+++ b/src/dispextern.h
@@ -148,6 +148,14 @@ #define NativeRectangle XRectangle
 typedef Emacs_Pixmap Emacs_Pix_Context;
 #endif
 
+#ifdef HAVE_IOS
+#include "iosgui.h"
+/* iOS display info.  */
+typedef struct ios_display_info Display_Info;
+typedef Emacs_Pixmap Emacs_Pix_Container;
+typedef Emacs_Pixmap Emacs_Pix_Context;
+#endif
+
 #ifdef HAVE_PGTK
 #include "pgtkgui.h"
 /* Following typedef needed to accommodate the MSDOS port, believe it or not.  */
diff --git a/src/sysdep.c b/src/sysdep.c
index a8320e1..d00ab02 100644
--- a/src/sysdep.c
+++ b/src/sysdep.c
@@ -30,6 +30,10 @@
 #include <sys/random.h>
 #include <unistd.h>
 
+#ifdef __APPLE__
+#include <TargetConditionals.h>
+#endif
+
 #include <c-ctype.h>
 #include <close-stream.h>
 #include <pathmax.h>
@@ -55,8 +59,12 @@
 # include <sys/proc.h>
 #endif
 
-#ifdef DARWIN_OS
+/* libproc.h is only available on macOS, not iOS */
+#if defined DARWIN_OS && TARGET_OS_OSX
 # include <libproc.h>
+# define HAVE_LIBPROC 1
+#else
+# define HAVE_LIBPROC 0
 #endif
 
 #ifdef __FreeBSD__
@@ -4268,8 +4276,14 @@ system_process_attributes (Lisp_Object pid)
 
 #elif defined DARWIN_OS
 
-#define HAVE_RUSAGE_INFO_CURRENT (__MAC_OS_X_VERSION_MIN_REQUIRED >= 101000)
-#define HAVE_PROC_PIDINFO (__MAC_OS_X_VERSION_MIN_REQUIRED >= 1050)
+/* libproc features are only available on macOS with libproc.h */
+#if HAVE_LIBPROC
+# define HAVE_RUSAGE_INFO_CURRENT (__MAC_OS_X_VERSION_MIN_REQUIRED >= 101000)
+# define HAVE_PROC_PIDINFO (__MAC_OS_X_VERSION_MIN_REQUIRED >= 1050)
+#else
+# define HAVE_RUSAGE_INFO_CURRENT 0
+# define HAVE_PROC_PIDINFO 0
+#endif
 
 Lisp_Object
 system_process_attributes (Lisp_Object pid)
@@ -4315,6 +4329,7 @@ system_process_attributes (Lisp_Object pid)
   if (gr)
     attrs = Fcons (Fcons (Qgroup, build_string (gr->gr_name)), attrs);
 
+#if HAVE_LIBPROC
   char pathbuf[PROC_PIDPATHINFO_MAXSIZE];
   char *comm;
 
@@ -4327,6 +4342,9 @@ system_process_attributes (Lisp_Object pid)
     }
   else
     comm = proc.kp_proc.p_comm;
+#else
+  char *comm = proc.kp_proc.p_comm;
+#endif
 
   decoded_comm = (code_convert_string_norecord
 		  (build_unibyte_string (comm),
diff --git a/src/xdisp.c b/src/xdisp.c
index 1234567..abcdef 100644
--- a/src/xdisp.c
+++ b/src/xdisp.c
@@ -13146,7 +13146,14 @@ resize_mini_window (struct window *w, bool exact_p)
   /* Nil means don't try to resize.  */
   if ((NILP (Vresize_mini_windows)
        && (NILP (resize_mini_frames) || !FRAME_MINIBUF_ONLY_P (f)))
-      || (FRAME_X_P (f) && FRAME_OUTPUT_DATA (f) == NULL))
+      || (FRAME_X_P (f) && FRAME_OUTPUT_DATA (f) == NULL)
+#ifdef HAVE_IOS
+      /* Don't try to resize initial frame's minibuffer during iOS startup.  */
+      || FRAME_INITIAL_P (f)
+      /* Don't try to resize if frame's glyphs aren't initialized yet.  */
+      || !f->glyphs_initialized_p
+#endif
+      )
     return false;
 
   if (FRAME_MINIBUF_ONLY_P (f))
@@ -13169,6 +13176,17 @@ resize_mini_window (struct window *w, bool exact_p)
 	  set_buffer_internal (XBUFFER (w->contents));
 	}
 
+#ifdef HAVE_IOS
+      /* On iOS, during early startup the buffer may not be fully
+         initialized.  Skip resize if BEGV >= ZV (empty or invalid).  */
+      if (BEGV >= ZV)
+        {
+          if (old_current_buffer)
+            set_buffer_internal (old_current_buffer);
+          return false;
+        }
+#endif
+
       init_iterator (&it, w, BEGV, BEGV_BYTE, NULL, DEFAULT_FACE_ID);
 
       /* Compute the max. number of lines specified by the user.  */
@@ -27417,6 +27428,11 @@ display_menu_bar (struct window *w)
   if (FRAME_NS_P (f))
     return;
 #endif /* HAVE_NS */
+#ifdef HAVE_IOS
+  /* iOS uses its own menu handling via SwiftUI/UIKit.  */
+  if (FRAME_IOS_P (f))
+    return;
+#endif /* HAVE_IOS */
 
 #ifdef HAVE_HAIKU
   if (FRAME_HAIKU_P (f))
diff --git a/src/termhooks.h b/src/termhooks.h
index 5e86105..42eba00 100644
--- a/src/termhooks.h
+++ b/src/termhooks.h
@@ -65,6 +65,7 @@ #define EMACS_TERMHOOKS_H
   output_pgtk,
   output_haiku,
   output_android,
+  output_ios,
 };
 
 /* Input queue declarations and hooks.  */
@@ -540,6 +541,7 @@ #define EVENT_INIT(event) (memset (&(event), 0, sizeof (struct input_event)), \
     struct pgtk_display_info *pgtk;		/* pgtkterm.h */
     struct haiku_display_info *haiku;		/* haikuterm.h */
     struct android_display_info *android;	/* androidterm.h */
+    struct ios_display_info *ios;		/* iosterm.h */
   } display_info;
 
 
@@ -960,6 +962,9 @@ Return font cache data for the specified terminal.  The historical
 #elif defined (HAVE_ANDROID)
 #define TERMINAL_FONT_CACHE(t)\
   (t->type == output_android ? t->display_info.android->name_list_element : Qnil)
+#elif defined (HAVE_IOS)
+#define TERMINAL_FONT_CACHE(t)\
+  (t->type == output_ios ? t->display_info.ios->name_list_element : Qnil)
 #endif

 extern struct terminal *decode_live_terminal (Lisp_Object);
