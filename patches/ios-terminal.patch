diff --git a/src/terminal.c b/src/terminal.c
index 668b884..2306f39 100644
--- a/src/terminal.c
+++ b/src/terminal.c
@@ -474,6 +474,10 @@ DEFUN ("terminal-live-p", Fterminal_live_p, Sterminal_live_p, 1, 1, 0,
       return Qhaiku;
     case output_android:
       return Qandroid;
+#ifdef HAVE_IOS
+    case output_ios:
+      return Qios;
+#endif
     default:
       emacs_abort ();
     }
