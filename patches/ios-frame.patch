diff --git a/src/frame.h b/src/frame.h
index 9db6877..f96ff46 100644
--- a/src/frame.h
+++ b/src/frame.h
@@ -656,6 +656,7 @@
     struct pgtk_output *pgtk;		/* From pgtkterm.h. */
     struct haiku_output *haiku;		/* From haikuterm.h. */
     struct android_output *android;	/* From androidterm.h.  */
+    struct ios_output *ios;		/* From iosterm.h.  */
   }
   output_data;
 
@@ -952,6 +953,11 @@
 #else
 #define FRAME_ANDROID_P(f) ((f)->output_method == output_android)
 #endif
+#ifndef HAVE_IOS
+#define FRAME_IOS_P(f) false
+#else
+#define FRAME_IOS_P(f) ((f)->output_method == output_ios)
+#endif
 
 /* FRAME_WINDOW_P tests whether the frame is a graphical window system
    frame.  */
@@ -973,6 +979,9 @@
 #ifdef HAVE_ANDROID
 #define FRAME_WINDOW_P(f) FRAME_ANDROID_P (f)
 #endif
+#ifdef HAVE_IOS
+#define FRAME_WINDOW_P(f) FRAME_IOS_P (f)
+#endif
 #ifndef FRAME_WINDOW_P
 #define FRAME_WINDOW_P(f) ((void) (f), false)
 #endif
diff --git a/src/frame.c b/src/frame.c
index abc123..def456 100644
--- a/src/frame.c
+++ b/src/frame.c
@@ -318,6 +318,10 @@ DEFUN ("framep", Fframep, Sframep, 1, 1, 0,
       return Qhaiku;
     case output_android:
       return Qandroid;
+#ifdef HAVE_IOS
+    case output_ios:
+      return Qios;
+#endif
     default:
       emacs_abort ();
     }
@@ -5861,8 +5865,8 @@ gui_set_first_window_line (struct frame *f, Lisp_Object arg, Lisp_Object oldval)
 #endif
 }
 
-#if (defined HAVE_PGTK || defined HAVE_NTGUI \
-     || defined HAVE_HAIKU || defined HAVE_NS)
+#if (defined HAVE_PGTK || defined HAVE_NTGUI || defined HAVE_HAIKU \
+     || defined HAVE_NS || defined HAVE_IOS)
 void
 gui_set_alpha (struct frame *f, Lisp_Object arg, Lisp_Object oldval)
 {
@@ -7076,6 +7080,9 @@ syms_of_frame (void)
   DEFSYM (Qpgtk, "pgtk");
   DEFSYM (Qhaiku, "haiku");
   DEFSYM (Qandroid, "android");
+#ifdef HAVE_IOS
+  DEFSYM (Qios, "ios");
+#endif
   DEFSYM (Qvisible, "visible");
   DEFSYM (Qbuffer_predicate, "buffer-predicate");
   DEFSYM (Qbuffer_list, "buffer-list");

