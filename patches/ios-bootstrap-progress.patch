diff --git a/src/lread.c b/src/lread.c
--- a/src/lread.c
+++ b/src/lread.c
@@ -52,6 +52,11 @@ along with GNU Emacs.  If not, see <https://www.gnu.org/licenses/>.  */
 #include "nsterm.h"
 #endif
 
+#ifdef HAVE_IOS
+/* Bootstrap progress reporting for iOS.  */
+extern void ios_report_load_progress (const char *filename);
+#endif
+
 #include <unistd.h>
 #include <fcntl.h>
 
@@ -1317,6 +1322,14 @@ load_with_autoload_queue (Lisp_Object file,
     Vloads_in_progress = Fcons (found, Vloads_in_progress);
   }
 
+#ifdef HAVE_IOS
+  /* Report loading progress to the iOS bootstrap UI.
+     This must be done here BEFORE Vload_source_file_function check,
+     because that function may return early and bypass later code.  */
+  if (STRINGP (found))
+    ios_report_load_progress (SSDATA (found));
+#endif
+
   /* All loads are by default dynamic, unless the file itself specifies
      otherwise using a file-variable in the first line.  This is bound here
      so that it takes effect whether or not we use
