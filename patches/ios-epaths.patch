diff --git a/src/epaths.in b/src/epaths.in
index 2f4f94f..e1803bf 100644
--- a/src/epaths.in
+++ b/src/epaths.in
@@ -20,6 +20,32 @@ along with GNU Emacs.  If not, see <https://www.gnu.org/licenses/>.  */
 
 #if !defined HAVE_ANDROID || defined ANDROID_STUBIFY
 
+#ifdef HAVE_IOS
+
+/* iOS paths - runtime paths resolved from the app bundle.
+   These extern variables are set by ios_init_paths() at startup.
+   Preserve the extra spaces, or epaths.in will not be generated correctly.  */
+
+ # define  PATH_LOADSEARCH	(ios_lisp_directory ? ios_lisp_directory : "")
+ # define  PATH_REL_LOADSEARCH	""
+ # define  PATH_SITELOADSEARCH	""
+ # define  PATH_DUMPLOADSEARCH	(ios_lisp_directory ? ios_lisp_directory : "")
+ # define  PATH_EXEC	(ios_exec_directory ? ios_exec_directory : "")
+ # define  PATH_DATA	(ios_etc_directory ? ios_etc_directory : "")
+ # define  PATH_BITMAPS	""
+ # define  PATH_DOC	(ios_etc_directory ? ios_etc_directory : "")
+ # define  PATH_INFO	""
+ # define  PATH_GAME	((char const *) 0)
+ # define  PATH_X_DEFAULTS	""
+
+extern char *ios_lisp_directory;
+extern char *ios_etc_directory;
+extern char *ios_exec_directory;
+
+#else /* !HAVE_IOS */
+
+/* Standard Unix/macOS paths - these are substituted by configure.  */
+
 /* Together with PATH_SITELOADSEARCH, this gives the default value of
    load-path, which is the search path for the Lisp function "load".
    Configure (using "make epaths-force") sets this to
@@ -81,6 +107,8 @@ along with GNU Emacs.  If not, see <https://www.gnu.org/licenses/>.  */
 /* Where Emacs should look for the application default file. */
 #define PATH_X_DEFAULTS "/usr/lib/X11/%L/%T/%N%C%S:/usr/lib/X11/%l/%T/%N%C%S:/usr/lib/X11/%T/%N%C%S:/usr/lib/X11/%L/%T/%N%S:/usr/lib/X11/%l/%T/%N%S:/usr/lib/X11/%T/%N%S"
 
+#endif /* !HAVE_IOS */
+
 #else
 
 /* Replace the defines above with links to files in the assets
